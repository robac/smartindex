<?php

class ThemeCollector {
    public function collect($initDir) {
        $pattern = $initDir.'/*';
        $ret = "";
        foreach(glob('libs') as $file) {
            $ret .= $file.'poch';
        }
        return $ret;
    }
}
